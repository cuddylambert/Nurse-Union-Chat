# ===============================
# UnionExplain Environment Config
# ===============================

# === OpenAI API ===
# Used for GPT-4o-mini responses and retrieval augmentation
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# === Database ===
# Postgres connection (local Docker or cloud DB)
# Example (Docker default):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/unionexplain
# Example (Render/Fly.io):
# DATABASE_URL=postgresql://user:password@host:5432/unionexplain

# === Redis ===
# For caching sessions and retrieval history
REDIS_URL=redis://default:password@localhost:6379

# === Auth (optional login for saved Q&A history) ===
# If using Clerk or Auth.js:
CLERK_API_KEY=
CLERK_FRONTEND_API=

# === Deployment Mode ===
# local | production
NODE_ENV=local

# === App Settings ===
# Default union (used when no upload)
DEFAULT_UNION_CODE=NX
DEFAULT_UNION_NAME="California Nurses Association"
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# === Logging & Monitoring ===
LOG_LEVEL=info
ENABLE_TELEMETRY=true
