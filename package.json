{
  "name": "nurse-union-chat",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r --if-present lint",
    "format": "prettier --write .",
    "typecheck": "pnpm -r --if-present typecheck",
    "ingest:nx": "node ingest/nx_crawl.js"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12"
  }
}
